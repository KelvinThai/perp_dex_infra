apiVersion: apps/v1
kind: Deployment
metadata:
  name: market-maker
  namespace: perp-dex
spec:
  replicas: 1
  selector:
    matchLabels:
      app: market-maker
  template:
    metadata:
      labels:
        app: market-maker
    spec:
      containers:
      - name: market-maker
        image: custom-perp-bots:latest
        args: ["--bot", "market-maker", "--market", "3"]
        env:
        - name: RPC_ENDPOINT
          valueFrom:
            configMapKeyRef:
              name: rpc-config
              key: RPC_ENDPOINT
        - name: PROGRAM_ID
          value: "6prdU12bH7QLTHoNPhA3RF1yzSjrduLQg45JQgCMJ1ko"
        - name: KEYPAIR_PATH
          value: "/keys/admin-keypair.json"
        volumeMounts:
        - name: admin-key
          mountPath: /keys
          readOnly: true
        resources:
          requests:
            cpu: "100m"
            memory: "256Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
      volumes:
      - name: admin-key
        secret:
          secretName: admin-keypair
